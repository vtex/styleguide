import useTableContext from '../../EXPERIMENTAL_Table/hooks/useTableContext'

const useTableTreeContext = () => {
  const tableContext = useTableContext()

  return tableContext
}

export default useTableTreeContext
